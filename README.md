# nuqta-landing-page

## Programs CMS

Program content is managed in Sanity and rendered on `/program/` from the public
Sanity CDN. Configure the browser fetch in `assets/js/sanity-config.js`, and see
`docs/program-editor-guide.md` for the marketing workflow.

Run the Studio locally with:

```sh
npm install
npm run studio:dev
```
